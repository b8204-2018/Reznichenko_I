#include <iostream>
#include <stdio.h>
#include <pch.h>
#include "darts.h"

using namespace std;
int main() {
	int x, y, goal;
	scanf_s("%d", &x, &y);
	printf("%s", "Score: ");
	printf("%d", goal);
	return 0;
}